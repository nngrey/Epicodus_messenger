<h3><%= @contact.name %></h3>
<%= @contact.number %>
<br>
<br>
<p><%= link_to "Edit", edit_contact_path(@contact), :class => "btn btn-primary"%></p>
<p><%= link_to "Delete Contact", contact_path(@contact),
                            data: { confirm: 'You sure?' },
                            method: "delete",
                            :class => "btn btn-danger" %></p>
